%p#notice= notice

%p
  %b Title:
  = @article.title
%p
  %b Description:
  = @article.description
%p
  %b Content:
  = @article.content

%h2 Commentaires

- if @comments.size == 0
  %p Aucun commentaire.
- else
  %dl
    - @comments.each do |comment|
      %dt= comment.user.fullname + ", le " + comment.updated_on.to_formatted_s(:long_ordinal)
      %dd= comment.content

= link_to 'Edit', edit_article_path(@article)
\|
= link_to 'Back', articles_path
